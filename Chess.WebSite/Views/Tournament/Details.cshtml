@using Chess.Infrastructure.DTO
@model TournamentDetailsDto

<tr>
    <td>
            @await Component.InvokeAsync("PlayerList",
                new {
                    testa = "aa"
                });
    </td>
</tr>



@* <div class="card">
    <div class="card-body">
        @Model.Name
        @Model.MaxPlayers
    </div>
</div>

<!-- Tournaments -->
<div class="card">
    <div class="card-header">
        Wynik turnieju
    </div>
    <div class="card-body">
<!-- Tournaments -->
        <table class="table">
            <thead>
                <tr>
                    <th>
                        PlayerId
                    </th>
                    <th>
                        Username
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model.Players) {
                <tr>
                    <td>
                        <a asp-controller="Match" asp-action="GetPlayerMatch" asp-route-id="@item.PlayerId">@Html.DisplayFor(modelItem => item.PlayerId)</a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Username)
                    </td>
                </tr>
        }
            </tbody>
        </table>
    </div>
</div>


<!-- Tournaments -->
<div class="card">
    <div class="card-header">
        Lista rozegranych spotkań
    </div>
    <div class="card-body">
<!-- Tournaments -->
        <table class="table">
            <thead>
                <tr>
                    <th>
                        PlayerId
                    </th>
                    <th>
                        Username
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model.Players) {
                <tr>
                    <td>
                        <a asp-controller="Match" asp-action="GetPlayerMatch" asp-route-id="@item.PlayerId">@Html.DisplayFor(modelItem => item.PlayerId)</a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Username)
                    </td>
                </tr>
        }
            </tbody>
        </table>
    </div>
</div>

 *@
